# 自撮り美顔カメラアプリ 仕様書

最終更新: 2026-09-07 / 作成PC: ROOM2F

## 背景

既存の美顔カメラアプリは課金要素が強く、満足に使えない。
iPhone・Android 両方で使える自作アプリを作る。**課金ゼロで完結させる**ことが必須条件。

## 方式の決定（2026-09-07）

**PWA（ホーム画面追加型 Web アプリ）として作る。**

### なぜネイティブアプリではないか

| | 費用 | 家族・友人への配布 | 備考 |
|---|---|---|---|
| PWA | **¥0** | URL を送るだけ | ← **採用** |
| iOS ネイティブ（無料枠） | ¥0 | **実質不可能** | 7日ごと再署名。他人の端末には入れられない |
| iOS ネイティブ（有料） | $99/年 | TestFlight | 画質最良だが課金必須 |

- Mac は所有しているが、無料枠は「自分の端末専用」の仕組みで、**家族・友人に配るという要件と両立しない**
- PWA なら iPhone / Android が1つのコードで同時に完成する
- 補正エンジン本体（GLSL シェーダ）は、将来ネイティブへ移行する場合もほぼそのまま流用できる

### PWA の制約と、本件での実害

| 制約 | 本件での実害 |
|---|---|
| 暗所に弱い（ナイトモード等の OS 側処理が使えない） | **小**。主な用途が「明るい屋内・日中の屋外」のため |
| 解像度が動画相当（最大フルHD〜4K程度） | 小。SNS 用途なら十分 |
| カメラロールへ直接保存不可 → 共有シート経由で2タップ | 小。自撮りライト等でカバー |
| 音量ボタンでシャッターが切れない | 小 |
| 起動が1〜2秒もたつく | 小 |

→ **将来「夜の画質が許せない」となった時点で、$99 を払ってネイティブ移行を検討する。**

## 機能仕様

### 補正内容（顔の変形は行わない）

顔の輪郭を細くする・目を大きくする等のワープ処理は**第1版に含めない**（難易度が一段上がるため）。

| 項目 | 内容 |
|---|---|
| 肌なめらか | バイラテラルフィルタ＋周波数分離。質感を残して「加工感」を抑える |
| 明るさ / コントラスト | 基本のトーン調整 |
| 肌トーン | 色白寄り・血色寄りの調整 |
| くま消し | 目の下の明度を持ち上げる（顔ランドマーク利用） |
| 色味フィルター | 3D LUT 方式。プリセットの追加がファイル1枚で済む |
| 目・唇・眉の保護 | 顔検出で肌のみをぼかし、目鼻立ちはシャープに保つ |

### 操作 UI

- **プリセット＋スライダーの両方**
  - 通常はワンタップのプリセット（ナチュラル / しっかり / 色白 / 血色 / オフ）
  - 設定を開くと項目ごとのスライダーで微調整、自分の設定を保存して使い回せる
- 補正は**リアルタイム**（撮る前のプレビューに反映済み）
- 補正前後の比較（長押しで元画像を表示）

### 撮影まわり

- **イン／アウト両カメラ対応**（切替ボタン）
- **左右反転（ミラー）は設定で選択可能**
- 写真中心。動画は後のフェーズで追加
- 自撮りライト（画面を白く発光させて顔を照らす）
- グリッド表示、セルフタイマー（3秒 / 10秒）

### 配布

- **GitHub Pages**（無料・HTTPS 付き。カメラ API は HTTPS 必須）
- 家族・友人には URL を送るだけ。更新は push するだけで全員に反映
- URL は推測されにくい名前にする

## 技術構成

| 要素 | 採用 | 理由 |
|---|---|---|
| 言語・構成 | 素の HTML / JS（ES modules）、ビルド不要 | GitHub Pages にそのまま置ける。依存を増やさない |
| 画像処理 | WebGL2 + 自作 GLSL シェーダ | スマホ内蔵 GPU で 30fps を確保。CPU 処理では数 fps しか出ない |
| 顔検出 | MediaPipe Face Landmarker | 無料・オフライン・数百点のランドマーク |
| 色味 | 3D LUT テクスチャ | 軽量。フィルター追加がファイル追加だけで済む |
| 保存 | Web Share API（共有シート）＋ ダウンロード fallback | iOS でカメラロールに保存する現実的な最善手 |
| オフライン化 | manifest + service worker | ホーム画面追加、圏外でも起動 |

※ GPU はスマホ本体に内蔵されているものを使う。開発 PC は関係なく、追加のハードも不要。

## 実装フェーズ

| Phase | 内容 | 目的 |
|---|---|---|
| **1** | カメラ表示 + WebGL パススルー + 撮影・保存 | **実機で画質を確認する。ここが最大のリスク検証点** |
| 2 | 肌なめらか・明るさ・トーンのシェーダ、スライダー UI | 補正の中核 |
| 3 | 顔検出を組み込み、肌マスク・目唇の保護・くま消し | 「のっぺり顔」を回避する |
| 4 | LUT フィルター、プリセット、設定の保存 | 使い勝手 |
| 5 | PWA 化、GitHub Pages 公開 | 配布 |
| 6 | 動画録画 | 追加要望 |

**Phase 1 の実機確認で「画質が許容できない」と判明した場合のみ、ネイティブ移行を再検討する。**

## 未決定事項

- 使用する実機（iPhone の機種 / Android の機種）
- GitHub アカウントの有無
- シャッター音を自前で鳴らすかどうか

---

# Phase 1 実施結果（2026-09-07）

## テスト環境

- 端末: SHARP SH-M29（Android 16 / Chrome 152 / Snapdragon 7s Gen 2 系 / RAM 6GB）
- 接続: `adb reverse tcp:8000 tcp:8000` でUSB経由。localhost 扱いになるため HTTPS 不要

## 実測値

| 要求解像度 | 実際に取れた解像度 | FPS |
|---|---|---|
| フルHD | 1080×1920 | **63.7** |
| 最大（4K要求） | **2160×3840** | **57.4** |

- 撮影・保存とも成功（`/sdcard/Download/selfie_20260907_132508.jpg` 204KB）
- 画質は許容範囲との評価

## 結論

**PWA 方式で問題なし。Phase 2 に進む。**

Web 経由でも 4K が取得でき、しかも 57fps を維持できた。これは当初の想定より良い結果。
FPS に余裕があるため、補正シェーダを載せる余地は十分ある。

## Phase 2 に持ち越す設計判断

**プレビューと撮影で処理解像度を分ける。**

上記 FPS は無加工（パススルー）の値。バイラテラルフィルタ等を 4K 全画素にかけると負荷は桁違いに増える。
そのため以下の2段構えにする。

- プレビュー: 画面表示に必要な解像度（フルHD 程度）で補正 → 滑らかさを優先
- 撮影時: フル解像度（4K）で一度だけ補正 → 画質を優先

## Phase 1 で判明した実装上の知見

1. **Android の共有シートには「画像を保存」が無い。**
   保存は `<a download>` によるダウンロードが正解で、`Download` フォルダはギャラリーからも見える。
   iOS は逆に共有シートの「画像を保存」だけがカメラロールへの唯一の経路。
   → OS で保存方法を分岐させる実装にした（`IS_IOS` 判定）。共有は別ボタンに分離。

2. **Chrome のカメラ権限は二重構造。**
   OS → Chrome アプリの権限と、Chrome → サイトの権限は別物。
   前者が無いと、サイト側で「許可」を押しても「このサイトは権限を要求できません」となる。
   テスト端末では Chrome に OS レベルのカメラ権限が無く、これが原因だった。

3. **権限の失敗はオリジン単位で記録される。**
   一度失敗した `localhost:8000` は、`127.0.0.1:8000` で開き直すと新規サイト扱いになり回避できる。

## 未実施

- **iPhone 15 Pro での確認**（GitHub Pages 等の HTTPS 公開先が必要なため保留中）

---

# Phase 2 実施結果（2026-09-07）

## 実装した内容

- 補正シェーダ（`js/shaders.js`）
  - バイラテラルフィルタ（横・縦の2パス）で輪郭を残したまま肌をなめらかに
  - 周波数分離により肌の質感を一部戻し、のっぺり感を回避
  - YCbCr による肌色判定で、肌以外に補正がかからないようにマスク
  - 明るさ・コントラスト・彩度・色温度・肌の明るさ
- 4パス構成のレンダラ（`js/renderer.js`）
- プリセット5種（オフ／ナチュラル／しっかり／色白／血色）＋マイ設定の保存
- スライダー8項目による微調整
- 画面長押しで補正前と見比べ
- 肌判定の可視化（調整パネル内のチェックボックス）
- 設定の自動保存（localStorage）

## 実測値（SH-M29）

| | 補正なし | 補正あり（改善前） | 補正あり（改善後） |
|---|---|---|---|
| フルHD | 63.7 | 60 | 60 |
| 4K | 57.4 | **30** | **57** |

評価: 補正の見た目は「普通に使える」。肌判定も正常に機能。

## 4K が 30fps に落ちた原因と対処

プレビュー中もカメラのフル解像度（830万画素）を処理していたのが原因。
画面は 1080×2340 しかないため、4倍の画素を計算しても見た目は変わらず frame rate だけが落ちていた。

対処として、処理解像度を用途で分けた。

| | 出力解像度 | ぼかしの解像度 |
|---|---|---|
| プレビュー | 長辺 1920 まで縮小 | 長辺 720 |
| 撮影の瞬間だけ | カメラのフル解像度（4K） | 長辺 1440 |

保存される写真は 4K のまま。プレビューだけを軽くしたので画質の低下はない。

## 検証用ページ

`test-shader.html` — 合成したテスト画像に補正をかけ、
座標系の反転が起きていないか、輪郭を保ったままぼかせているか、
肌判定が正しく効いているかを目視で確認できる。実機を使わずにシェーダを検証するために作成。

---

# 公開と iPhone 実測（2026-09-07）

## 公開

- アカウント: `ks-kit`（2026-09-07 作成）
- リポジトリ: https://github.com/ks-kit/selfie-cam （public）
- **公開URL: https://ks-kit.github.io/selfie-cam/**
- 初回ビルドは約60秒で完了

コミットには GitHub の匿名アドレス（`<id>+ks-kit@users.noreply.github.com`）を使う設定を
リポジトリローカルに入れてある。公開リポジトリのコミット履歴にメールアドレスを残さないため。

## 実測比較

| 端末 | フルHD | 4K |
|---|---|---|
| SH-M29（Snapdragon 7s Gen 2 系） | 60 | 57 |
| **iPhone 15 Pro（A17 Pro）** | **60** | **60** |

**両OSとも 4K が実用域**であることが確定した。
当初リスクと見ていた「PWA は解像度が動画相当に制限される」「処理が重くて frame rate が出ない」は、
いずれも杞憂だった。

iOS では保存時に共有シートが開き、「画像を保存」でカメラロールに入ることを確認。
OS で保存方法を分岐させた実装が意図通り動いている。

## 実機フィードバックによる変更

1. **プリセットを一段強くした。** 当初の設定は全体に効きが弱く、「しっかり」がちょうど
   「ナチュラル」の感覚だった。元の「ナチュラル」相当は「ひかえめ」として残した。
2. **保存時の鏡像設定を廃止し、常に実際の向きで保存するようにした。**
   純正カメラと同じ挙動になり、写り込んだ文字も鏡文字にならない。
   プレビューのみ鏡像で表示する。

## 残タスク

- Phase 3: 顔検出（くま消し、目や唇の保護）— 今は肌色判定で代用できている
- Phase 4: 色味フィルターのプリセット（LUT 方式）
- Phase 6: 動画撮影

---

# 全画面化（PWA 化の完成）（2026-09-07・家KS）

## 問題

実機で上下にブラウザのバーが出たままで、画面が狭い。

## 原因

**`manifest.json` も Service Worker も存在せず、PWA として成立していなかった。**
`index.html` にあったのは `apple-mobile-web-app-capable` の 1 行のみ。

| 端末 | 症状 |
|---|---|
| iPhone | 上記 meta が効くため「ホーム画面に追加」すればバーは消えていた。URL を直接開くと出る |
| Android | manifest が無いため Chrome がインストールを提示せず、ホーム画面に追加してもタブとして開きバーが残る |

## 対処

1. `manifest.json` を追加。`display: "standalone"`、`display_override: ["fullscreen", "standalone"]`
2. アイコンを生成（`icons/` に 192・512・maskable 2種・apple-touch-icon）
3. `index.html` に manifest とアイコンをリンクし、`mobile-web-app-capable` と
   `apple-mobile-web-app-status-bar-style: black-translucent` を追加
4. `sw.js`（Service Worker）を追加し、`index.html` から登録

### GitHub Pages がサブパス配信である点への対応

公開先が `https://ks-kit.github.io/selfie-cam/` と**サブディレクトリ**なので、
`start_url` / `scope` を `"/"` にすると壊れる。**すべて相対パス**（`"./"`, `"manifest.json"`,
`"sw.js"`, `"icons/..."`）で書いてある。ここは触らないこと。

### Service Worker をネットワーク優先にした理由

キャッシュ優先にすると、push で直したはずの `app.js` が実機に古いまま残り
「直したのに変わらない」が起きる。**取得はネットワーク優先、失敗時のみキャッシュ**にして、
更新が必ず届くことを優先した。オフライン起動は、成功したレスポンスを都度キャッシュへ
写すことで担保している。

### `black-translucent` が安全な理由

ステータスバーの下までコンテンツが描画されるようになるが、
`css/style.css` の `#app` に既に `padding-top: env(safe-area-inset-top)` が入っているため、
上端の診断バーが隠れることはない。

## 検証結果（家KS・Chrome 152）

| 確認項目 | 結果 |
|---|---|
| manifest / アイコン 4種 / apple-touch-icon の取得 | すべて 200 |
| Service Worker | 登録 → activated → ページを制御 |
| プリキャッシュ | HTML/CSS/JS3種/manifest/アイコン2種 の 9 件 |
| **オフライン起動** | **サーバーを停止した状態でリロードし、正常表示** |

### ハマった点

**デスクトップアプリ内蔵ブラウザでは Service Worker が登録できない。**
`sw.js` 自体は 200・`text/javascript` で取得できるのに、登録だけが
`An unknown error occurred when fetching the script` で失敗する。
内蔵ブラウザ側の制約であり、コードの問題ではない。**実 Chrome で確認すること。**

## 制約（仕様として受け入れる）

**バーを消せるのはホーム画面のアイコンから起動した場合だけ。**
ブラウザのタブとして開いている限り、Web ページ側からバーは消せない。

---

# 画面を広く使う調整（2026-09-07・家KS）

## 問題

全画面化したあとも「下の方が余っている」。

## 実測（iPhone 15 Pro・393×852・スタンドアロン起動）

| 領域 | 変更前 | 変更後 |
|---|---:|---:|
| 上セーフエリア | 59px | 59px |
| 診断バー | 54px | **0px（既定で非表示）** |
| **映像エリア** | **532px（62%）** | **631px（74%）** |
| コントロール | 173px | 162px |
| 下セーフエリア（帯） | 34px | 0px（コントロール内で確保） |

**映像エリアが +99px（+18.6%）。**

## 対処

1. **診断バー（状態・解像度・FPS・カメラ）を既定で非表示にした。** 開発用の情報で
   常時出す必要がない。調整パネルの「診断バー」チェックで出せる。状態は localStorage に保存。
2. **オプション行を廃止し、中身を調整パネルへ移した。** 解像度と鏡像表示は撮影中に
   触るものではないため。「調整」ボタンはプリセット行の右端に固定配置した
   （プリセット行は横スクロールするので、その外に置いてスクロールで消えないようにしてある）。
3. **下端のセーフエリアを `#app` ではなく `#controls` の padding で確保するようにした。**
   `#app` で空けるとコントロールの下に帯ができ、その分だけ映像が縮む。

## 副産物: 左右の黒帯も減った

カメラは縦長（1080×1920）で、それを `object-fit: contain` で収めている。
映像エリアが縦に伸びると横方向にも拡大されるため、**左右の黒帯が 94px → 38px** に減った。

## ハマった点

### 1. 古い CSS がキャッシュされたまま計測していた

`python -m http.server` は `Cache-Control` を返さないのでブラウザが独自判断でキャッシュする。
新しい CSS ルールが 1 つも効かない状態で計測してしまい、
「`#presets` の指定が無視される」「`#diag.hidden` が効かない」と誤診しかけた。

```js
await fetch('css/style.css', {cache: 'reload'});  // これで強制的に取り直す
location.reload();
```

**ローカルで CSS を変えたら、まずキャッシュを疑うこと。**

### 2. 絶対配置の子は padding 領域も覆う

`#preview` は `position: absolute; inset: 0` で `#app` の中にある。
絶対配置の基準は containing block の **padding ボックス**で、これは **padding 領域を含む**。
つまり `#app` に `padding-top: env(safe-area-inset-top)` があっても、
`#preview` は画面の一番上まで広がる。

そのため**撮影結果画面のヘッダ（「← 撮り直す」「等倍で見る」）がステータスバーに
潜り込んでいた**（この変更以前からの不具合）。`.pv-head` / `.pv-foot` 側で
個別に `env(safe-area-inset-*)` を確保して直した。

計測値: 「撮り直す」の上端 69px（セーフエリア 59px 以上）、
「保存」の下端から画面下まで 44px（ホームインジケータ 34px 以上）。

---

# 補正の見直し A: 肌マスクの締め直し（2026-09-07・家KS）

## きっかけ

理想の仕上がりの before/after を提示され、あわせて
**「今のアプリの処理だと全体にぼやける」**という指摘を受けた。

## 目標画像から読み取った変化

| 変化 | 備考 |
|---|---|
| **髭（口周り・顎）の青黒さが薄くなる** | 最大の変化。広い範囲の低周波の影 |
| 頬の細かいシミ・点が消える | 大きなほくろ1つは残る＝目立つ特徴は保持 |
| 毛穴が目立たない | 鼻の質感とハイライトは残る＝過剰なのっぺりではない |
| 全体に明るく、やや血色寄り | 明るさ・暖色・彩度がわずかに上 |
| 鼻の穴・唇・ほうれい線の輪郭は保持 | |

## 「ぼやける」の原因

### 原因1: 肌マスクが肌以外まで拾っていた

`skinMask` を代表的な色で評価したところ、肌以外にも広く反応していた。

| 対象 | 変更前 | 変更後 |
|---|---:|---:|
| 肌（頬）・目の下のくま | 1.00 | 1.00 |
| 肌（暗い室内） | 0.95 | 0.82 |
| **眉・髪** | **0.61** | **0.04** |
| 濃い髪 | 0.18 | 0.00 |
| 鼻の穴 | 0.30 | 0.00 |
| ベージュの壁 | 0.22 | 0.00 |
| グレーの壁 | 0.12 | 0.00 |
| 髭の影 | 0.67 | 0.31 |
| 唇 | 1.00 | 1.00 |

**眉と髪が 6 割ぼけていた**のが主犯。暗部を除外するガードが `y: 0.06→0.20` と緩く、
髪や眉がすり抜けていた。

### 原因2: 細部を戻す量が少なかった

マスクが掛かった場所で失われる高周波の割合:

| プリセット | 肌 | 眉・髪（変更前） |
|---|---:|---:|
| ナチュラル | 53% | 32% |
| しっかり | 78% | 47% |

混ぜ込まれる「ぼかし」はプレビューでは半分の解像度で計算した画なので、その分も効く。

## 変更内容

| 箇所 | 変更前 | 変更後 |
|---|---|---|
| `skinMask` 色距離 | `smoothstep(0.155, 0.045, d)` | `smoothstep(0.115, 0.040, d)` |
| `skinMask` 暗部ガード | `smoothstep(0.06, 0.20, y)` | `smoothstep(0.15, 0.30, y)` |
| `sigmaColor` | 0.16 | 0.11 |
| `detail`（ひかえめ／ナチュラル／しっかり） | 0.55 / 0.38 / 0.22 | 0.60 / 0.48 / 0.34 |

**暗部ガードの値は慎重に選んだ。** より強い `0.18→0.36` だと眉髪は 0.00 になるが、
**かなり暗い室内での肌が 0.79 → 0.28 まで落ちる**。採用した `0.15→0.30` は
眉髪 0.04 / 暗い室内の肌 0.82 で、暗所を犠牲にしない。

`sigmaColor` 0.16 では色差 0.16 の画素にまだ 0.61 の重みが残り、
眉毛と肌の境目のような中くらいの輪郭を越えて混ざっていた。

## 検証方法（再利用できる）

**色パッチを並べたテスト画像を `maskOnly` で描画し、読み戻す。** 実機もカメラも要らない。

```js
const { Renderer } = await import('./js/renderer.js');
// 64x64 の色パッチを横に並べた canvas を作り、
const r = new Renderer(out); r.init(); r.resize(src.width, src.height, 0.5);
r.draw(src, { maskOnly: true, flipX: false });
r.gl.readPixels(...)   // R成分がそのままマスク値
```

この方法で 11 色すべてが事前の計算値と一致することを確認した。
`skinMask` は純粋な色の関数なので、同じ式を Python に写して先に候補を比較できる。

## 残る限界

- **唇（1.00）と暖色の背景（木目など）は色だけでは分離できない。** 顔検出（Phase 3）が要る
- **髭は 0.31 に下がったが、これは正しい分担。** 髭は「ぼかして消す」のではなく
  「暗さを持ち上げて消す」もので、次の B（低周波パス）が担当する

## 次（B）

低解像度のガウスぼかしを1組追加して「その辺りの肌の平均色」を作り、
平均より暗い画素を平均へ寄せる（上げるだけ・上限つき）。
髭・くま・くすみ・小さなシミは全部「周りの肌より局所的に暗い」ので、これでまとめて扱える。
マスクを平均色の側で判定すれば、髭の領域も周囲の肌のマスクを引き継ぐ。

---

# 補正の見直し B: 髭・くま・くすみ（2026-09-07・家KS）

## 考え方

髭・くま・くすみ・小さなシミには**「周りの肌より、その場所だけ暗い」**という共通の性質がある。
バイラテラルは細かい凹凸しか触らないので、この低い周波数の暗さは消せない。
そこで**「その辺りの肌の平均色」を別に作り、平均より暗い分を戻す**。

## 肌だけの平均色をどう作るか（ここが要点）

単純にぼかすと髪や背景が混ざって使いものにならない。3段階で作る。

1. 肌マスクを色に掛けて書き、**マスクそのものを alpha に入れる**（`FRAG_SKINPACK`）
2. RGBA のままガウスでぼかす（`FRAG_GAUSS`・横→縦）
3. 合成側で **rgb を alpha で割る**

これで肌だけの重み付き平均になり、髪や背景は alpha が小さいので混ざらない。
そして**髭自身のマスクは 0.31 と低い**ので、髭の領域の平均は「周囲の肌の色」に寄る。これが狙い。
A でマスクを締めて髭が 0.67 → 0.31 に下がったことが、ここで効いている。

## パス構成（4 → 7）

| | 内容 | 解像度 |
|---|---|---|
| 0 | 映像 → origFBO | 出力解像度 |
| 1,2 | バイラテラル 横→縦 | 長辺 720（撮影時 1440） |
| 3 | 肌だけを取り出す | **長辺 128 固定** |
| 4,5 | ガウス 横→縦 | 長辺 128 固定 |
| 6 | 合成 | 出力解像度 |

追加分は長辺 128 なので負荷はごくわずか。
**出力解像度によらず固定の大きさ**にしてあるので、プレビューでも撮影でも同じ効き方になる。

## ハマった点

### 1. area をそのまま重みに使うと、一番効かせたい場所で効かない

ぼかした alpha（＝その辺りが肌である度合い）をそのまま重みに掛けたところ、
**髭の中心で area が 0.52 まで落ち、持ち上げが半分になっていた。**
髭自身のマスクが低いことは「平均の重み」としては正しいが、
「顔の領域か」の判定としては誤り。`smoothstep(0.12, 0.45, area)` を通して 0/1 に寄せた。

実測: 髭の持ち上げ **+8.4 → +16.7**

### 2. 唇が +18 も持ち上がった

唇は「周囲より暗い」ので、そのままだと持ち上げの対象になり色が飛ぶ。
唇は周囲の肌より**赤（cr）が明確に高い**ので、cr の超過で抑えるガードを入れた。

実測: 唇 **+18 → 0**

色ムラの平均化の方は最初から「周囲より彩度が低い画素だけ」を対象にしていたので
唇は影響を受けていなかった。**持ち上げ側にも同種の守りが要る**、という話。

## 検証結果

合成したパッチを「しっかり」相当（shadow 0.62 / even 0.50）で処理した実測値。

| 対象 | 明るさの変化 | 青みの変化 | |
|---|---:|---:|---|
| **髭** | **+16.7** | **-8** | 狙いどおり |
| **目の下のくま** | **+19.3** | -1 | 狙いどおり |
| 唇 | 0 | 0 | 守れている |
| 眉 | +2.3 | -1 | ほぼ無変化 |
| 鼻の穴 | 0 | 0 | 守れている |
| 平らな肌 | 0 | 0 | 副作用なし |

## あわせて直した: プレビューと保存で補正の効き幅が違っていた

`u_radius` は「ぼかしバッファ上の画素数」。
プレビューはバッファ長辺 720、撮影は 1440 なので、**同じ radius でも画に対する効き幅が
撮影側で半分**になっていた。「プレビューだけを軽くしたので画質の低下はない」という
当初の意図と食い違う。`RADIUS_REF = 720` で正規化して揃えた。

**知覚的な差がどれくらいかは詰め切れなかった。** 合成した縞模様で波長別に測ろうとしたが、
低解像度バッファへの縮小でエイリアシングが出て、測定値がそれに支配された。
幾何的な不一致は計算で確実なので、測定を待たずに直してある。

## 実機フィードバックによる修正: 顔が平べったくなった

初版の B は**効きすぎだった**。実機での感想は「顔全体が平べったくのっぺり」
「A のみくらいがちょうど良かった」。

### 原因

**持ち上げが自然な陰影まで拾っていた。**
鼻の脇・あごの下・頬の丸みも「周囲の肌より暗い」ので、髭やくまと区別されず
一緒に持ち上がり、顔から立体感が消えていた。

### 対処

**常に残す陰影の量 `SHADE_KEEP = 0.05` を引いてから持ち上げる**ようにした。

```glsl
float excess = max(yBase - yCol - SHADE_KEEP, 0.0);
col += min(excess, LIFT_MAX) * u_shadow * guard * redGuard;
```

上限で頭打ちにするのではなく**差し引き**にしたのが要点。
強い影ほど多く戻りつつ、陰影の順序（どこが濃いか）は保たれる。
あわせてプリセットの値も下げた（ナチュラル 0.40/0.30 → 0.30/0.18）。

### 修正後の実測

| 対象 | ナチュラル | しっかり |
|---|---:|---:|
| 平らな肌 | 0 | 0 |
| **自然な陰影（頬の丸み）** | **0** | **0** |
| やや強い陰影（鼻の脇） | +2.0 | +3.3 |
| くま | +5.0 | +8.9 |
| 髭 | +4.2 | +7.3 |
| 唇・鼻の穴 | 0 | 0 |

**自然な陰影がちょうど 0 になる**のがこの値の狙い。
以前は同じ場所が持ち上がっていたため、顔が平坦に見えていた。

### 教訓

**「周囲より暗い」だけを手がかりにすると、消したい暗さと残したい暗さを区別できない。**
唇（cr の超過で除外）、鼻の穴・眉（明るさの下限で除外）に続いて、
自然な陰影も明示的に守る必要があった。**守りを3つ重ねて、ようやく狙った所だけに効く。**

## 最終的な値（実機で決定）

陰影を守る仕組みを入れたあとでも、**ナチュラルの 0.30 はまだ強かった**。
実機で詰めた結果 **`shadow` は 0.10** に落ち着いた。

| プリセット | shadow | even |
|---|---:|---:|
| ひかえめ | 0.06 | 0.10 |
| ナチュラル | **0.10** | 0.18 |
| しっかり | 0.18 | 0.32 |
| 色白 | 0.12 | 0.20 |
| 血色 | 0.10 | 0.20 |

`even`（色ムラ）については指摘が出なかったので据え置き。

**「髭・くま」スライダーの上限は 1.0 → 0.5 に狭めた。** 実用域が 0〜0.2 なのに
上限が 1.0 では、使える範囲がツマミの左端に潰れて調整できないため。

### この機能について分かったこと

**当初 0.40 で設計していたものが、実機では 0.10 が適正だった。4倍ずれていた。**
合成パッチでの検証は「どこに効くか」の確認には有効だったが、
**「どれくらい効かせるべきか」は実機の顔でしか決められない。**
数値上は自然に見えても、実際の顔では過剰になる。

## 残タスク（更新）

- Phase 3: 顔検出 — 唇と暖色の背景（木目など）は色だけでは肌と分離できない
- Phase 4: 色味フィルターのプリセット（LUT 方式）
- Phase 6: 動画撮影

---

# 操作の手数を減らす（2026-09-07・家KS）

「起動ボタンを押すのと、保存に一手間かかるのは改善できないか」という指摘への対応。

## 手数（タップ数）

| | 変更前 | 変更後 |
|---|---:|---:|
| iPhone | 5 | **4** |
| Android | 4 | **2** |

内訳（変更前）: アイコン → 「カメラを起動」 → シャッター → 「保存」 →（iOSのみ）「画像を保存」

## 1. 起動ボタンを省く

`getUserMedia` はユーザー操作を要求されることがあるため、初回はボタンが要る。
ただし**一度許可された後は自動で開ける環境がある**ので、2回目以降は試すようにした。

```js
if (perm === 'denied') return;                    // 明示的に拒否されている
if (perm !== 'granted' && !state.camOk) return;   // 初回は必ずボタンから
await startCamera({ auto: true });
```

- `navigator.permissions.query({name:'camera'})` が使える環境（Chrome）は、その結果で判断する
- **Safari は camera を照会できない**ので、「過去に一度でも開けたか」を localStorage に
  記録しておき（`camOk`）、それで判断する
- **断られたらエラー画面ではなく起動ボタンに戻す**（`startCamera({auto:true})` の `auto`）。
  つまり失敗しても従来どおりの動きになるだけで、試すことに副作用がない

## 2. 「撮ったらすぐ保存」トグル

オンにすると、シャッター後にプレビューを挟まず保存へ進む。

保存処理を `storeShot()` として切り出し、`'ok' | 'cancel' | 'blocked'` を返すようにした。
**`'ok'` 以外なら今までどおりプレビューを見せる**ので、撮った写真を取りこぼすことはない。

- **Android**: ダウンロードは確実に通るので、シャッター1タップで Download フォルダへ直行
- **iPhone**: 共有シートはタップ直後にしか開けず、撮影処理を挟むと弾かれる。
  弾かれた場合はプレビューに落ちるので、実質これまでどおりになる可能性が高い

## 3. iPhone のボタンの重複を解消

iOS では「保存」も共有シートを開くため、「共有」と実質同じ動作をしていた。
iOS では「共有」を隠し、「保存」を **「保存・共有」** に改めた。

## 消せない手数（仕様上の限界）

**iOS の Web アプリは写真ライブラリに書き込めない。**
カメラロールに入れる唯一の経路が共有シートなので、
**共有シートの「画像を保存」のタップは、どうやっても消せない。**

## 検証について

自動起動そのものは、カメラのある実機でしか確認できない。
検証に使った内蔵ブラウザはカメラ権限が `denied` だったが、
**その場合に自動起動を見送り、エラー画面を出さずに起動ボタンのまま留まる**ことは確認した。

## 実機の結果（iPhone・2026-09-07）

### 1. 自動起動は iOS では効かない（仕様上の限界と確定）

**iOS Safari は `getUserMedia` にユーザー操作を要求する。** 権限が許可済みでも同じ。
自動起動の試行は弾かれ、設計どおりエラーを出さずに起動ボタンへ戻った。

**ただし許可ダイアログは出なくなった。** 権限自体は保持されるので、
2回目以降は「ボタンを1回タップするだけ」になる。

**このボタンは消せない**ので、代わりに**起動画面のどこをタップしても始まる**ようにした。
ボタンを狙う必要がなくなる。ボタンへのタップもオーバーレイへ上がってくるので、
待ち受けはオーバーレイ側の一つでよい（二重に起動しないことを確認済み）。

Android は `permissions.query` が使えて権限も永続するため、自動起動が効く見込み。

### 2. 「撮ったらすぐ保存」は iPhone でも効いた

**以前の記録（「撮影処理を挟むと共有シートが弾かれる」）を覆す結果。**
撮影の `await` を挟んだあとでも共有シートが開き、プレビューは飛んだ。

Safari の挙動が変わったのか、当時の条件が違ったのかは分からない。
**撮影が長引く（4K など）と間に合わない可能性は残る**ので、
弾かれたらプレビューに落ちる作りはそのまま残してある。

### 手数の最終結果

| | 変更前 | 変更後 |
|---|---:|---:|
| iPhone | 5 | **3**（アイコン → 画面タップ → シャッター → 「画像を保存」のうち、保存は共有シート内の1タップ） |
| Android | 4 | **2**（見込み） |

iPhone の内訳: アイコン → 画面のどこかをタップ → シャッター → 共有シートの「画像を保存」。
**共有シートの1タップだけは、Web アプリが iOS の写真ライブラリに書き込めない以上、消せない。**

---

# セルフタイマーと直前の一枚（2026-09-07・家KS）

## セルフタイマー

**オフ → 3秒 → 5秒 → 10秒** を巡回するボタン。設定は保存される。

置き場所は**映像の右上に浮かせた**（純正カメラと同じ位置）。
最初はプリセット行の右に置いたが、`調整` と並べるとプリセットの表示幅を食って
「しっかり」以降がスクロールしないと見えなくなったため、映像の上へ移した。

### 秒読み中の扱い

- 残り秒は映像の中央に大きく出す。**当たり判定は持たせない**（`pointer-events: none`）ので、
  構えている間の「長押しで補正前と比較」を邪魔しない
- **シャッターが中止ボタンを兼ねる**。秒読み中は赤くなる
- 停止・カメラ切替・解像度変更でも秒読みは無効になる（`stopCamera()` の先頭で解除）

## 直前の一枚（サムネイル）

撮影後、映像の左下に直前の一枚を小さく残す。押すとその写真が開く。純正カメラと同じ挙動。

**「撮ったらすぐ保存」でプレビューを飛ばしたときの、写真への戻り道**にもなっている。
この2つは組み合わせて効く。

## 重なり順

`.overlay`（起動画面・エラー画面）に `z-index: 10` を与えて、
タイマーとサムネイル（6）より前面にした。

これをしないと、カメラ停止中に**サムネイルが起動画面の上に乗ってしまい**、
「画面のどこをタップしても始まります」と言いながらその一角だけ写真が開く、
という食い違いが起きる。

## タイマー＋「撮ったらすぐ保存」は iOS でも通った

実装時は「共有シートはタップ直後にしか開けないので、タイマーを挟むと弾かれるだろう」と
予想していたが、**実機（iPhone）では 3 秒のタイマーを挟んでも共有シートが開いた。**

**iOS の共有シートに関する事前の予想は、これで2回続けて外れている。**
（撮影処理を挟むと弾かれる → 通った／タイマーを挟むと弾かれる → 通った）
**この制約は思っていたより緩い。** ただし条件が変われば弾かれる可能性は残るので、
弾かれたらプレビューに落ちる作りはそのまま残してある。

## サムネイルは時間で消す

出しっぱなしにせず、**5秒後にふわっと縮みながら消える**ようにした（フェード 0.55秒）。

- 撮り直すたびに数え直す
- **消えかけを掴んでも開ける。** タップしたら消えるのを止めて、プレビューを閉じたら数え直す

### 純正アプリの挙動（調べた結果）

- **iOS の「カメラ」アプリのサムネイルは消えない。** ギャラリーへの入口を兼ねているため、
  アプリを開いている間はずっと残る
- **消えるのはスクリーンショットのサムネイル**の方で、体感5秒ほどで自動的に消える

つまり要望の「5秒くらいで消える」はスクリーンショットの挙動に近い。
このアプリは画面を広く使いたいので、そちらに合わせた。

---

# Phase 4: 色味フィルター（2026-09-07・家KS）

## LUT 画像ではなく式で実装した

当初の計画は「LUT 方式」だったが、**式で書く方に変えた。**

| | LUT 画像 | 式 |
|---|---|---|
| 追加で取得する画像 | 数十〜数百 KB | **なし** |
| 値を直したいとき | 画像を作り直して push | **数字を1つ直して push** |
| 外部の .cube を読める | できる | できない |

**このアプリは「実機で見ながら値を詰める」進め方をしている**（美顔補正の shadow を
0.40 → 0.10 に詰めた経緯がまさにそれ）。その繰り返しに対して、
毎回 LUT 画像を焼き直すのは分が悪い。手作りの数本なら式の方が速い。

外部の .cube LUT を読み込みたくなったら、そのときサンプラーを足せばよい。
今の作りはその邪魔をしない。

## 中身

どのフィルターも同じ4つの操作の組み合わせで書いてある。

| 操作 | 効果 |
|---|---|
| `fade` | 黒を持ち上げる（フィルムの褪せた感じ） |
| `sTint` / `hTint` | 暗部と明部に別々の色を乗せる（スプリットトーン） |
| `con` | コントラスト |
| `sat` | 彩度 |

7種類: なし / フィルム / クリア / やわらか / ノスタルジー / クール / モノクロ

**適用は一番最後**（肌の補正とトーンが終わった画に対して全体の色を決める）。
強さは 0〜1 で調整できる。

## 検証（色パッチを流して読み戻した実測値）

| フィルター | 明部 235,232,228 | 暗部 60,58,62 | 肌 222,178,152 |
|---|---|---|---|
| なし | 235,232,228 | 60,58,62 | 222,178,152 |
| フィルム | 253,244,229 | 60,60,**72** | 234,189,157 |
| クリア | 253,251,248 | 48,49,60 | 241,188,157 |
| やわらか | 233,227,220 | **81,77,77** | 220,181,156 |
| ノスタルジー | 251,244,232 | 75,69,68 | 228,194,167 |
| クール | 243,244,248 | 48,50,**70** | 230,183,163 |
| モノクロ | 249,249,249 | 56,56,56 | **200,200,200** |

フィルムとクールで暗部の青が上がり、やわらかで黒が持ち上がり、
モノクロで R=G=B になっている。強さ 0.5 は「なし」と「フィルム」のちょうど中間だった。

## UI

**映像の左上にボタン、押すと映像の下側に一覧が出る。**
タイマーを右上に置いたのと同じ考えで、**コントロール側の幅を食わない**ようにした。

- シャッターを押すと一覧は閉じる（出たままだと画が隠れるため）
- 「なし」のときは強さのスライダーを無効にする
- **美顔がオフでも色味は効く。** `bypass` は両方オフのときだけにしてある

## 状態の持ち方

`look` と `lookAmount` は `state.params` ではなく `state` に置いた。
params に入れると**美顔プリセットを切り替えるたびに色味までリセットされる**ため。
描画時に `state.look` / `state.lookAmount` を渡している。

---

# Phase 3（顔検出）実施結果（2026-09-08・医院PC ROOM2F）

## やった理由

肌の判定を色だけで行っていたため、**唇と暖色の背景（木目の壁など）が肌と区別できない**という
積み残しがあった。顔の輪郭の内側だけを対象にすれば両方まとめて外せる、という狙い。

## 使ったもの

- MediaPipe Face Landmarker（`@mediapipe/tasks-vision` **1.0.1**）
  - 最新版は 0.10 系ではなく **1.0.1**。思い込みで書かず、npm registry で確認すること
  - モデル `face_landmarker.task` は **3.58MB**（実測）
- 検証は `face-test.html` + `js/face-test.js`（本体に触らず単独で試せる）

## 速度の実測（SH-M29・毎フレーム検出・補正なし）

| 条件 | FPS | 検出1回 |
|---|---|---|
| 輪郭478点 / GPU / 等倍 | 23 | 48ms |
| 輪郭478点 / GPU / 長辺640に縮小 | 20 | 48ms |
| 輪郭478点 / **CPU** / 等倍 | 20 | 48ms |
| 顔の枠だけ（BlazeFace 230KB）/ GPU | 30 | 30ms |

**試して駄目だったこと（同じ道を辿らないための記録）**

1. **入力を縮小しても速くならない。** 長辺640に落としても 48ms のまま変わらず、
   縮小のための描画コストだけ増えて逆に遅くなった。
   MediaPipe が内部で入力を固定サイズへ正規化していると考えられる。
2. **CPU と GPU で差が出ない。** どちらも 48ms。
   GPU 転送のオーバーヘッドを疑ったが違った。
3. **軽量な検出器に替えても劇的には速くならない。** 顔の枠だけを返す BlazeFace は
   モデルが 1/15（230KB）なのに 48ms → 30ms にしかならず、
   毎フレーム（16.6ms 以内）には遠い。輪郭 478 点を捨てる価値はないと判断した。

**結論: この端末では検出 1 回あたり 48ms は削れない。毎フレーム検出は不可能。**
そのため **4 フレームに 1 回だけ検出し、間は前回のマスクを使い回す**方式にした。
マスクの縁はぼかしてあるので、数フレームの遅れは目に見えない。

## 本体へ統合した後の実測

| 端末 | | 顔検出オフ | 顔検出オン |
|---|---|---|---|
| SH-M29 | フルHD | 60fps | **50fps** |
| SH-M29 | 4K | 57fps | **37fps** |
| **iPhone 15 Pro** | フルHD | 60fps | **60fps** |
| **iPhone 15 Pro** | 4K | 60fps | **60fps** |

**iPhone 15 Pro では顔検出を入れても frame rate が全く落ちない。**
判断が分かれるのは Android 側だけで、機種によって「常時オンでよい」場合もありうる。
既定オフの切り替え式にしてあるのは、どちらの端末でも使えるようにするため。

## 効果と判断

得られた効果は**「唇のシャープさが微妙に上がる」程度**にとどまった。

**この代償に対して常時オンにする価値はない、と判断。**
**既定オフの切り替え式（調整パネルの「顔検出を使う」）として残す。**

そのため以下も見送っている。

- **モデルのローカル配置はしない。** 常用しないものに 3.58MB のキャッシュを積む意味がない。
  CDN から読むため、**オフラインでは顔検出だけが使えない**（本体は従来どおり動く）。

## 実装の要点

- `js/face.js` … 検出とマスク生成。`FaceMask` クラス
  - マスクは長辺 256 の 2D キャンバス。顔の輪郭を白で塗り、目・眉・唇を黒で抜く
  - 縁は `ctx.filter = blur(...)` でぼかし、検出の遅れやズレが縁に出ないようにしている
  - **顔が見つからないときは全面白に戻す。** 補正が急に消えるより、
    顔検出が無かった頃と同じ挙動になるほうが自然なため
- `js/shaders.js` … 合成パスで `mask *= texture(u_faceMask, fuv).r`
  - **座標系に注意。** 映像テクスチャと描画先とで上下が逆なので `fuv.y = 1.0 - v_uv.y`、
    さらに鏡像表示のときは `fuv.x` も反転する。ここが最もバグりやすい
- モデルの読み込みはチェックを入れた時点で初めて走る（起動を遅くしないため）

## iOS で長押しメニューが割り込む問題（2026-09-08 修正）

画面の長押しは「補正前と見比べる」操作に割り当てているが、
**iOS では長押しで「コピー / 画像を保存」のメニュー（callout）が出てしまい、比較にならなかった。**

🔴 **`contextmenu` の `preventDefault` では止まらない。** iOS のこのメニューは
そのイベントを経由しないため、既に入れてあった抑制は効いていなかった。

対策は iOS 専用の CSS プロパティ。

```css
html, body, #app, #stage, #view, #controls, #tune, button, label, canvas {
  -webkit-touch-callout: none;
  -webkit-user-select: none;
  user-select: none;
}
```

**撮影結果の画像（`#pv-img`）だけは `default` に戻して長押しを残している。**
共有シートで保存できなかったときに「長押しして保存」を案内しているため、
そこまで殺すと保存の逃げ道が無くなる。

## 残タスク

- Phase 6: 動画撮影（要望としては弱い）

---

# App Store で売る案の検討（2026-09-09・医院PC ROOM2F）

## きっかけとなった問い

> iPhone でこういう美顔補正アプリを、無料のまま広告を見ながら使っている人はかなりいるはず。
> 数百円の安い買い切りで公開すれば、そこそこ売れるのではないか。
> そもそも同じようなアプリは App Store にもうたくさんあるのか。

**結論：まず売れない。** 品質の問題ではなく、**存在を知られないため。**
ただし「サブスク疲れの層がいる」という読み自体は外れていない（後述）。

## 調べて分かったこと

### 市場は巨大で、相手は全部企業

- 美顔カメラアプリ市場は **年 50〜75 億ドル規模**（7,000 億〜1.1 兆円。調査会社により幅がある）、
  年 13〜14% 成長
- **アジア太平洋が 41.3%** を占める。日本は大手にとって主戦場であって、空白地帯ではない

| アプリ | 開発元 |
|---|---|
| YouCam Makeup | Perfect Corp |
| BeautyPlus / BeautyCam | Meitu（中国の上場企業） |
| Facetune | Lightricks（イスラエルのユニコーン） |
| B612 / SNOW | Snow Corporation（LINE / Naver 系） |
| AirBrush | Pixocial |
| Ulike | — |

加えて Snapchat / Instagram が **300 万本を超える AR レンズ**を抱えて同じ土俵にいる。

### 本当の壁は「作れるか」ではなく「見つけてもらえるか」

- 🔴 **App Store 収益の 90% 以上が上位 1% のアプリに集中している**
- 新規アプリ数は 2025 年に前年比 **31% 増**
- 検索アルゴリズムは**レビュー数・広告予算・専任 ASO チーム**を持つ側に有利にできており、
  個人開発者が「美顔カメラ」「自撮り」といった語で上位に出るのは現実的に不可能

### 買い切りが少ないのは市場の合理的な帰結

- **サブスクは買い切りの 4.5 倍の生涯収益**を生む。大手が一斉にサブスクへ移行したのには根拠がある
  （Facetune も以前は買い切りだった）
- ただし**買い切り・ライフタイム課金の比率は 6.4% → 10.3% に増加**している（2023→2025）。
  サブスク疲れへの揺り戻しは実際に起きており、**当初の見立て自体は間違っていない**

## それでも勝算があるとしたら

1. **「美顔カメラ」の土俵で戦わない。**
   大手が拾わないニッチなら競合がいない分、検索で見つけてもらえる。
   本業由来の切り口として、歯科の術前術後記録・口腔内写真・患者説明用の画像がある。
   市場が小さすぎて大手が来ない領域は、逆に「これしかない」状態を作れる
2. **先にファンを集めてからアプリを出す**（出してから探してもらうのは順序が逆）
3. **PWA のまま需要を検証する** ← コストゼロで今すぐできる

## 次に検討するときの前提

**アプリ化を考える前に、PWA のまま知人に配って使い続けられるかを見ること。**
URL を送るだけで配れるので、$99 も審査も Mac も要らず、需要の検証コストがゼロ。
**ここで使われないなら、App Store に出しても結果は同じ。**

なお 2026-09-08 には別角度（撮影の手数を減らす目的）でもアプリ化を検討し、
同じく「割に合わない」と結論している。**目的が「手数」でも「収益」でも、現時点の答えは同じ。**

## 出典

- https://www.mordorintelligence.com/industry-reports/beauty-camera-apps-market
- https://www.thebusinessresearchcompany.com/report/beauty-camera-apps-global-market-report
- https://mobiledevmemo.com/the-app-store-discoverability-squeeze/
- https://appopportunity.com/blog/indie-app-revenue-models-2026
- https://www.applyra.io/blog/complete-aso-guide-indie-developers
- https://ravi6997.medium.com/why-the-golden-age-of-indie-ios-apps-is-over-and-what-developers-must-do-now-8223542291fb

---

# 自動起動は Android では効いていた（2026-09-10・家KS）

## 経緯

実機の感想は「Android も起動のタップが必要」だった。しかし **USB 接続した SH-M29 を
adb で直接調べたところ、自動起動は正常に動いていた。**

## 実機で確認した手順

```
adb -s <id> shell input tap ...            # 画面を操作
adb -s <id> exec-out screencap -p > x.png  # 画面を撮る
```

**Chrome DevTools 経由（`adb forward tcp:9222 localabstract:chrome_devtools_remote`）は
応答が無く使えなかった。** ソケットは端末側に存在するのに HTTP が返らない。
**画面を撮って読む方法の方が確実だった。**

### 切り分けの決め手

`am force-stop` を **WebAPK のパッケージにかけても Chrome 本体は生き残る**ため、
再起動したつもりでも「前の状態が復帰しただけ」になる。これに二度騙されかけた。

確実な手順はこう。

1. アプリ内の「停止」を押す → 状態=停止・FPS=− ・起動画面が出た状態にする
2. `am force-stop` を **WebAPK と `com.android.chrome` の両方**にかける
3. 起動する

これで **タップなしに 状態=動作中・FPS 39.7** になった。**自動起動は効いている。**

## 「効かない」と見えた本当の原因

**`STORE_KEY` を上げると、`camOk`（一度でもカメラを開けた記録）まで一緒に消えていた。**

医院PCのセッションで `STORE_KEY` が `v10` に上がっており、そのとき記録も消えた。
その状態での起動は**設計どおり「初回」扱いでボタンが出る**。
つまり見えていたのは正しい動作で、**その1回だけを見て「効かない」と判断していた。**

## 対処

`camOk` を **`STORE_KEY` とは別のキー（`beautycam.camOk`）** に分けた。
これは設定ではなく端末の実績なので、プリセットの値を変えるたびに消える筋合いがない。
旧形式に `camOk: true` があれば一度だけ引き継ぐようにしてある。

検証: 旧データを置いてリロード → `beautycam.camOk` が作られ、
**設定側（`beautycam.v10`）を削除しても実績は残る**ことを確認した。

## あわせて入れた診断

**起動ボタンが出ている状態からは「条件を満たさず見送った」のか「試して断られた」のかが
区別できない。** 今回はそれで原因の特定が遅れたので、理由を状態表示に出すようにした。

- `待機中（自動: 見送り 権限=prompt 実績=なし）`
- `待機中（自動失敗: NotAllowedError ...）`

診断バーを出せば読める。

## 実測（SH-M29・2160×3264・顔検出オン）

**39.7〜40.7fps。** 仕様書の Phase 3 に記録した 4K + 顔検出 37fps とおおむね一致する。

## まとめ: 自動起動の対応状況

| | 自動起動 | 保存 |
|---|---|---|
| **Android** | **効く**（2回目以降） | **Download へ直行**（1タップ） |
| **iPhone** | **効かない**（iOS はユーザー操作が必須） | 共有シートの「画像を保存」が必須 |

---

# 訂正: 「起動のたびにタップが要る」の本当の原因（2026-09-10・家KS）

## 前節の結論は誤りだった

前節で「自動起動は Android で効いている。`camOk` が消えていたのが原因」と書いたが、
**日常的に起きていた現象の原因はそれではなかった。**

本当の原因は [js/app.js](js/app.js) に**初版から入っていた**この処理。

```js
// タブが隠れている間はカメラを止めて発熱と電池を抑える
document.addEventListener('visibilitychange', () => {
  if (document.hidden && state.running) {
    stopCamera();
    setState('中断（バックグラウンド）');
    el.startOverlay.classList.remove('hidden');
  }
});
```

**隠れたら止めるが、戻ってきても再開しない。**
ホームに戻して開き直すたびに起動画面が出てタップが要る、という状態だった。

### なぜ検証で気づけなかったか

**毎回 `am force-stop` で Chrome ごと落として起動していたため、この経路を踏んでいなかった。**
ページが読み直されて `tryAutoStart()` が走り、確かに自動起動していた。
**利用者の操作（ホームに戻して開き直す）は、ページを読み直さない別の経路だった。**

検証の手順が現実の使い方とずれていた。**再現手順は利用者の操作に合わせること。**

## 対処

自分でバックグラウンドのために止めたときだけ、戻ったら開き直す。

```js
if (state.pausedByHide) {
  state.pausedByHide = false;
  startCamera({ auto: true, note: '復帰' });
}
```

利用者が「停止」を押した場合は `pausedByHide` が立たないので勝手には開かない。

**実機で確認**: ホームに戻して開き直し → タップなしで 動作中・43.8fps・映像も生きている。

## 🔴 もう一つの発見: 更新が実機に最大10分届かない

**GitHub Pages は `Cache-Control: max-age=600` を返す。**
Service Worker をネットワーク優先にしていても、その `fetch` は端末の HTTP キャッシュを
経由するため、**push した更新が最大10分間実機に届かない。**

今回の調査でも、直したはずのコードが動かず原因究明が空回りした。

対処として、Service Worker の取得を**条件付きリクエスト**にした。

```js
request = new Request(req, { cache: 'no-cache' });
```

必ずサーバへ確認しに行き、変わっていなければ 304 が返るだけなので転送量はほとんど増えない。
オフライン時にキャッシュへ落ちる動作はそのまま。

## 実機を adb で操作するときの注意

**座標を固定してタップしてはいけない。** 画面がロックされていたことに気づかず
固定座標でタップし続けた結果、**Google レンズを開いてカメラ権限のダイアログを出してしまった**
（「戻る」で閉じたので権限は変更していない）。

**毎回 `screencap` で現在の画面を確認してから、その画像で位置を決めてタップすること。**

---

# 復帰まわりの仕上げ（2026-09-10・家KS）

## 「準備中」の表示

復帰の数秒が**固まったように見える**という指摘への対応。

原因は **canvas に前回の最後の1枚が残り続ける**こと。撮影時に `toBlob` で中身を読むため
`preserveDrawingBuffer: true` が要り、これは消せない。

カメラを開いている間は暗く覆って「カメラを準備中…」を出すようにした。
成功・失敗のどちらでも消える。**待ち時間そのものは変わらないが、
「壊れた」ではなく「読み込み中」に見えるようになった。**

## 🔴 真っ黒のまま手詰まりになるバグ（初版から潜在・実機で再現）

調べる過程で、**真っ黒・エラー表示なし・ボタンもなし**の状態を実機で作ってしまった。

```js
// 修正前
if (el.video.videoWidth > 0 || performance.now() - t0 > timeout) return resolve();
```

**時間切れでも成功と同じように解決していた。** その結果 `videoWidth = 0` のまま
`applyPreviewSize(0, 0)` → `renderer.resize(0, 0)` と進み、canvas が潰れて何も映らない。
`hideOverlays()` が走った後なので**起動画面もエラー画面も出ず、操作する手段が無くなる。**

さらに **rAF で待っていたのも誤り**。ページが隠れている間 rAF は止まるので、
復帰の途中で呼ばれると**時間切れの判定すら動かない**。

### 対処

- 成否を返すようにした（確定したら `true`、時間切れなら `false`）
- `setTimeout` と `loadedmetadata` で待つ（rAF に依存しない）
- 取れなければ失敗として扱い、自動復帰なら起動画面、手動ならエラー画面（再試行つき）へ

**教訓: 「待つ」関数は必ず成否を返すこと。** 時間切れを成功と区別しないと、
呼び出し側が壊れた状態のまま先へ進む。

## 残る待ち時間と、短縮したい場合の選択肢

カメラを開く時間そのものは端末側の処理で、Web アプリからは短縮できない。
解像度が最大（3840×2160）だと数秒かかる。

| 案 | 効果 | 代償 |
|---|---|---|
| 解像度を「フルHD」に下げる | 開くのが速くなる | 保存される写真が 4K でなくなる |
| バックグラウンドでも数十秒はカメラを止めない | 短い切り替えなら待ち時間ゼロ | カメラ使用中の表示が点いたままになる |

**どちらも未実施。** 「準備中」の表示で十分という判断（2026-09-10）。

なお**描画はバックグラウンドで自動的に止まっている**（`loop()` は `requestAnimationFrame`
で回っており、ページが隠れると呼ばれない）。`visibilitychange` で止めているのは
実質カメラだけなので、2案目の電池への影響は見た目より小さい。

---

# ズーム（2026-09-12・家KS）

## 方式の選択

| 方式 | iPhone | Android | 画質 |
|---|---|---|---|
| ハードウェアのズーム（`applyConstraints`） | ❌ Safari 非対応 | ⭕ 端末次第 | 最良 |
| **デジタルズーム（切り出し）** | ⭕ | ⭕ | 4K から切るので2倍までは実用 |
| 画角を広げる（1倍より広く） | ❌ | ❌ | **不可能**。センサーより広くは写せない |

**デジタルズームを採用した。** ハードウェアズームは iPhone で使えず、結局デジタルが要る。
両OSで同じ挙動になる方を土台にした。

撮影が 4K（2160×3264）なので、切り出しても実効解像度は残る。

| 倍率 | 実効解像度 |
|---|---|
| 1.0x | 2160×3264 |
| **2.0x** | **1080×1632（フルHD相当）** |
| 3.0x | 720×1088 ← ここを上限にした |

## 実装

映像を取り込む最初のパス（`VERT_SOURCE`）で中心を基準に切り出すだけ。

```glsl
uv = (uv - 0.5) / max(u_zoom, 0.0001) + 0.5;
```

**プレビューも撮影も同じパスを通るので、見たままが保存される。**

### 🔴 顔マスクにも同じ切り出しが要る

顔マスクは**切り出す前の映像**から作られ、合成パスで**出力側の座標**で参照している。
映像だけ切り出すと**マスクだけ位置がずれ**、唇や目の保護が変な場所に掛かる。
合成パスの `fuv` にも同じ変換を掛けてある（反転は中心対称なので順序は問わない）。

**検証**: 顔マスクの幅が 78 → 156 → 234px（1:2:3）と映像と同じ比率で追従することを確認。

## 操作

**2本指のピンチ。** 1本指の長押し（補正前との比較）とは自然に棲み分ける。
2本目が触れた時点で比較用のタイマーを止めている。

- 倍率は映像の下端中央に表示。**等倍のときは出さない**
- **バッジを押すと等倍に戻る**
- `#view` に `touch-action: none` を掛けて、ピンチをブラウザに取られないようにした
- **倍率は保存しない。** 起動のたび等倍から始まる（純正カメラと同じ）
- カメラを開き直すときも等倍に戻す（レンズが変われば画角も変わるため）

## 検証（合成画像・bypass で切り出しだけを見た）

| 倍率 | 中央の白い四角の幅 | 隅の目印 |
|---|---|---|
| 1.0x | 40px | 見える |
| 2.0x | 78px | 切り落とされる |
| 3.0x | 118px | 切り落とされる |

## 今後

ハードウェアズーム（`track.getCapabilities().zoom`）が使える端末では、
そちらに切り替えれば画質を落とさずに済む。**SH-M29 が対応しているかは未確認。**
デジタルと組み合わせる（ハード上限まではハード、それ以上はデジタル）のが理想。

---

# アイコンの刷新（2026-09-12・家KS）

## きっかけ

初版は暗い背景にティールのレンズという配色だった。
**「加工系アプリはピンクや原色が多いので地味すぎる」**という指摘から作り直した。

## 決まったもの

**絞り羽根＋きらめき × 放射グラデーション（中心ピンク → 外周パープル）＋上部の光沢。**
名前は「美顔カメラ」のまま。

## 選定の過程で分かったこと

**48px（ホーム画面の実寸）で評価しないと判断を誤る。**
大きく見て良いものが、小さくすると全く別物になる。

| | |
|---|---|
| **ビフォー／アフターの分割** | 大きいと面白いが **48px で差が完全に消える**。没 |
| 8枚羽根・繊細な線 | 上品だが **48px で細かすぎて潰れる** |
| 白地に反転 | 今っぽいが **明るい壁紙だと背景が溶ける** |
| 手鏡・しずく | 形は残るが **何のアプリか伝わらない** |
| **絞り羽根** | **白の面積が大きく要素が少ないので、小さくても形が残る** |

**きらめきは大きめにした。** 標準の大きさだと 48px でほぼ点になり、
「美顔」の意味が伝わらなかった。

## maskable の扱い

Android は端末によって円・角丸・しずく型と切り方が違う。
**図柄を 0.76 倍にして中央の安全域（中心から半径 0.4）に収めてある。**
きらめきの先端は中心から 0.381 で、ぎりぎり収まる。

## theme_color / background_color は黒のまま

起動時のスプラッシュに使われる色。**アプリ本体が黒基調**なので、
ここを紫にすると「紫のスプラッシュ → 黒のアプリ」と切り替わる瞬間に色が飛ぶ。
**アイコンだけ派手で中身は黒**という構成にしてある。

## 🔴 アイコンと名前は配る前に決めること

**iPhone はホーム画面に追加した時点のアイコンと名前が焼き付く。**
あとから変えても既に追加した端末には反映されない。削除して追加し直すしかない。

Android（WebAPK）は Chrome が manifest を見に行けば追従するが、
**反映まで数日かかることがある。**

今回は配布前だったので影響が無かった。**配った後だったら、相手の端末に
古いアイコンが残り続けていた。**
