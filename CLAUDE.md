# 美顔カメラアプリ — 作業の引き継ぎ

最終更新: 2026-09-08（家KS）

> **2026-09-07 に家KSで大きく進めた。** PWA 化・全画面表示・補正の作り直し・
> セルフタイマー・サムネイル・色味フィルターまで、12 コミット。
> **経緯と数値の根拠はすべて `仕様書.md` にある。触る前に読むこと。**

## これは何か

iPhone / Android の両方で使える、自撮り用の美顔補正カメラ。ブラウザで動く **PWA**。
既存アプリの課金要素が強くて満足に使えない、という不満から自作を始めたもの。

- **公開URL: https://ks-kit.github.io/selfie-cam/**
- リポジトリ: https://github.com/ks-kit/selfie-cam （public）
- GitHubアカウント: `ks-kit`（2026-09-07 作成）

**撮った写真は端末外に一切出ない。** 撮影も補正も保存もすべて端末内で完結し、
GitHub Pages は静的ファイルの置き場でしかない。

## なぜネイティブアプリではなく PWA なのか

「家族・友人にも配りたい」という条件があったため。
iOS ネイティブの無料枠は自分の端末専用の仕組みで、他人に配れない。
配るには Apple Developer（$99/年）が必要になり、「課金したくない」という当初の動機と矛盾する。

PWA なら課金ゼロ・Mac 不要・審査なしで、iPhone と Android が 1 つのコードで動く。
懸念だった画質と frame rate は、実測の結果いずれも問題なかった（下表）。

### 2026-09-08 に再検討した結果（結論は変わらず）

「App Store でアプリ化すれば手数の問題は全部解決するのでは」という問いを検討した。

**技術的には解決する**（起動タップも保存の共有シートも不要になる）。
**残っている手間は撮影1回あたり2タップ。** それに対する代償が、

- Apple Developer **$99/年**（毎年）
- Xcode を動かす Mac か macOS の CI
- 提出のたびに審査（今は push して1分で反映）
- **Android は別に書き直し**（PWA の「1つのコードで両OS」が失われる）

**割に合わないので今はやらない。**
将来やるとしても Capacitor 等でラップすれば**今の HTML/JS/WebGL をそのまま中身にできる**ので、
今 PWA を育てることが将来の選択肢を狭めない。
**再検討する価値が出るのは Phase 6（動画撮影）を本格的にやるとき。**

### 2026-09-09 に「売れるかどうか」で再検討した結果（結論は変わらず）

「無料＋広告のアプリに疲れている層は多いはず。安い買い切りで出せば売れるのでは」
という問いを検討した。**答えは「まず売れない」。品質ではなく、存在を知られないため。**

- 美顔カメラアプリ市場は **年 50〜75 億ドル規模**（調査会社により幅がある）。
  **アジア太平洋が 41%** を占め、日本は大手の主戦場
- 競合は Meitu（中国の上場企業）、Lightricks（Facetune・ユニコーン）、
  Snow Corporation（B612 / SNOW）、Perfect Corp（YouCam）など**すべて企業**。
  さらに Snapchat / Instagram が 300 万本超の AR レンズを抱えて同じ土俵にいる
- 🔴 **App Store 収益の 90% 以上が上位 1% のアプリに集中している。**
  新規アプリ数は 2025 年に前年比 31% 増。検索アルゴリズムは
  レビュー数・広告予算・専任 ASO チームを持つ側に有利にできている
- 買い切りが少ないのは市場の合理的な帰結。**サブスクは買い切りの 4.5 倍の生涯収益**を生む。
  ただし買い切り・ライフタイムの比率は 6.4% → 10.3%（2023→2025）と増えており、
  **「サブスク疲れの層がいる」という読み自体は外れていない**

**「美顔カメラ」の土俵で戦わないなら話は別。**
大手が拾わないニッチ（例：歯科の術前術後記録・口腔内写真・患者説明用）なら、
競合がいない分ふつうに検索で見つけてもらえる。

**ただしその前に、PWA のまま知人に配って使い続けられるかを見るのが先。**
URL を送るだけなので $99 も審査も Mac も要らず、需要の検証コストがゼロ。
**ここで使われないなら、App Store に出しても結果は同じ。**

数字の根拠と出典は `仕様書.md` の「App Store で売る案の検討」にある。

## 今どこまでできているか

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | カメラ表示・撮影・保存 | **完了** |
| 2 | 美顔補正（肌なめらか・髭くま・色ムラ・トーン・プリセット） | **完了** |
| 4 | 色味フィルター（7種・強さ調整つき） | **完了**（LUT 画像ではなく式で実装。理由は `仕様書.md`）。実機で機能を確認済み（2026-09-08） |
| 5 | GitHub Pages 公開 | **完了** |
| — | PWA 化・全画面表示・タイマー・サムネイル・手数削減 | **完了**（2026-09-07） |
| 3 | 顔検出 | **完了**（2026-09-08・医院PC）。ただし**既定オフの切り替え式**。理由は下記 |
| 6 | 動画撮影 | 未着手。「たまに撮る」程度の要望 ← 残っているのはこれだけ |

### Phase 3 の結論（2026-09-08）

**入れたが、常時オンにする価値はなかった。既定オフの切り替え式で残してある。**
調整パネルの「顔検出を使う」で入り切りする。

| 端末 | | 顔検出オフ | 顔検出オン |
|---|---|---|---|
| SH-M29 | フルHD | 60fps | 50fps |
| SH-M29 | 4K | 57fps | 37fps |
| iPhone 15 Pro | フルHD / 4K | 60fps | **60fps（落ちない）** |

**iPhone では frame rate が全く落ちないので、常時オンでも困らない。**
効果のほうが**「唇のシャープさが微妙に上がる」程度**なので、
既定はオフのままにしてある。切り替え式にしているのは端末差が大きいため。

🔴 **同じ道を辿らないための記録。速くしようとして全部失敗している。**

- **入力を縮小しても速くならない**（長辺640でも 48ms のまま。縮小の描画コストだけ増える）
- **CPU と GPU で差が出ない**（どちらも 48ms）
- **軽量な検出器（顔の枠だけ・230KB）に替えても 30ms 止まり**で、毎フレームには遠い

**検出 1 回 48ms はこの端末では削れない。** 4 フレームに 1 回だけ検出して使い回している。
詳細と数値の根拠は `仕様書.md` の「Phase 3（顔検出）実施結果」にある。

**モデル（3.58MB）は CDN から読む。ローカルには置いていない**ため、
オフラインでは顔検出だけが使えない（本体は従来どおり動く）。常用しないものに
キャッシュを積む意味がないと判断した。

### 次にやること

**残っているのは Phase 6（動画撮影）だけ。**ただし要望としては弱い。
当初やりたかったことは一通り実現できている。

**未確認の宿題**: 色味フィルター7種の強さが、実機の顔で適正かどうか。
色パッチ上は狙いどおりだが、**顔に当てた適正値は実機でしか決まらない**
（美顔の shadow を 0.40 → 0.10 に詰めた前例がある）。

### 実測値

| 端末 | フルHD | 4K |
|---|---|---|
| iPhone 15 Pro（A17 Pro） | 60fps | 60fps |
| SH-M29（Snapdragon 7s Gen 2 系） | 60fps | 57fps |

**設計の経緯・ハマった点・数値の根拠はすべて `仕様書.md` にある。作業前にそれを読むこと。**

---

## 別のPCで作業を始めるときの手順

### 0. まず Dropbox の同期が終わっているか確認する

🔴 **このフォルダは Dropbox 同期下で、`.git` も同期される。**
前に使ったPCの同期が終わる前に git を触ると壊れる。
**タスクトレイの Dropbox が「最新の状態」になってから始めること。**

確認はこの2つで足りる。

```
git fetch origin
git status -sb
```

**`## main...origin/main` だけが出て、`ahead` も `behind` も付いていなければ同期できている。**
`behind` が付いていたら、前のPCの push を取り込めていないか Dropbox の同期が途中。
`ahead` が付いていたら、前のPCで push し忘れた変更がある（その内容を確認してから進める）。

作業ツリーに身に覚えのない変更が出ている場合も、同期が途中の可能性が高い。
**その状態で commit しないこと。**

### 1. GitHub CLI の認証（そのPCで初回のみ）

認証情報は PC ごとに保存されるため、PC ごとに一度ずつ必要。

| PC | 状態 |
|---|---|
| 家KS（`DESKTOP-C8E115C`） | **設定済み**（2026-09-07・gh 2.100.0） |
| 医院 ROOM2F | **設定済みのはず**（このリポジトリの初版をここから push している）。`gh auth status` で確認する |
| 医院clear | 未確認 |

**まず `gh auth status` を実行すること。** `✓ Logged in as ks-kit` が出れば以下は不要。
出なければ次を行う。

```
winget install --id GitHub.cli --accept-source-agreements --accept-package-agreements
```

インストール後、**新しいターミナルを開いてから**:

```
gh auth login --hostname github.com --git-protocol https --web
```

- `Authenticate Git with your GitHub credentials?` → **Y**
- 表示されたワンタイムコードをメモして Enter → ブラウザでコードを入力 → Authorize
- `✓ Logged in as ks-kit` と出れば完了

認証できたかは `gh auth status` と `git push --dry-run origin main` で確認できる。
認証を担うのは gh 自身ではなく Git Credential Manager（`C:\Program Files\Git\etc\gitconfig` の `credential.helper = manager`）で、
`gh auth login` の途中の `Authenticate Git with your GitHub credentials?` に **Y** と答えるとそこへ認証情報が渡る。ここを N にすると push だけ通らない。

### 2. ローカルサーバーを立てる

カメラ API は **https か localhost でしか動かない**。`file://` で開いても真っ黒になるだけ。

```
python -m http.server 8000
```

→ ブラウザで `http://localhost:8000/`

### 3. 実機で試す

- **iPhone など** → 公開URLを開く。push すれば約 1 分で反映される
- **Android を USB 接続している場合** → `adb reverse tcp:8000 tcp:8000` を実行すると、
  実機の `http://127.0.0.1:8000/` からローカル版に繋がる。
  localhost 扱いになるので HTTPS の用意が要らない

### 4. 更新を公開に反映する

```
git add -A
git commit -m "変更内容"
git push
```

約 1 分で `https://ks-kit.github.io/selfie-cam/` に反映される。

---

## 今の画面構成（どこに何があるか）

コントロール側の幅は取り合いになるので、**撮影中に触るものだけを下に置き、
それ以外は映像の上に浮かせるか、調整パネルへ入れてある。**

| 場所 | もの |
|---|---|
| 映像の左上 | **色味**（押すと映像の下側にフィルター一覧＋強さ） |
| 映像の右上 | **タイマー**（押すたび オフ→3秒→5秒→10秒） |
| 映像の左下 | **直前の一枚**（5秒でふわっと消える。押すとその写真が開く） |
| 映像の中央 | タイマーの残り秒（当たり判定なし。長押し比較を邪魔しない） |
| コントロール1行目 | 美顔プリセット（横スクロール）＋右端に **調整** |
| コントロール2行目 | カメラ切替 / **シャッター** / 停止 |
| 調整パネルの上部 | 解像度・鏡像で表示・撮ったらすぐ保存・診断バー |
| 調整パネルの本体 | スライダー10本 |

- **シャッターはタイマーの中止ボタンも兼ねる**（秒読み中は赤くなる）
- **起動画面はどこをタップしても始まる**（iOS はカメラ起動にユーザー操作が必須で、
  このタップだけは消せない。だからボタンを狙わなくてよいようにした）
- **Android は2回目以降タップ不要**（2026-09-10 に SH-M29 で確認）。
  初回だけボタンが要る。iPhone は毎回必要
- **バックグラウンドに回ると発熱対策でカメラを止め、戻ったら開き直す。**
  再開処理が無かったため、以前は開き直すたびにタップが要る状態だった
- **開き直している間は「カメラを準備中…」で覆う。** canvas には前回の最後の1枚が
  残る（撮影で中身を読むため消せない）ので、覆わないと固まったように見える
- 🔴 **「待つ」関数は必ず成否を返すこと。** `waitForVideoSize` が時間切れを成功と
  区別していなかったため、映像の寸法が取れないまま進んで**真っ黒・エラーなし・
  ボタンなしの手詰まり**になっていた（2026-09-10 に実機で再現して修正）。
  **`requestAnimationFrame` で待つのも不可**。隠れている間は止まるので時間切れも効かない

## 🔴 値を変えたら STORE_KEY と CACHE を上げる

プリセットの数値やパラメータの意味を変えたときは、**必ず2つセットで上げる。**

| ファイル | 定数 | 2026-09-08 時点 |
|---|---|---|
| `js/app.js` | `STORE_KEY` | `beautycam.v10` |
| `sw.js` | `CACHE` | `selfie-cam-v21` |

> この表は上げるたびに古くなる。**迷ったら実ファイルを見ること**
> （`grep "STORE_KEY = " js/app.js` / `grep "CACHE = " sw.js`）。

- **`STORE_KEY`** を上げないと、端末に残った古い設定が読み込まれて**新しい既定値が実機に出ない**。
  「直したのに変わらない」の原因はたいていこれ
- **`CACHE`** を上げると古いキャッシュが破棄される。Service Worker はネットワーク優先なので
  上げなくても更新は届くが、不要な残骸を消すために揃えて上げている

上げると**端末に保存された設定（マイ設定を含む）は消える。** それでよいときだけ上げること。

**「一度でもカメラを開けた」記録だけは `beautycam.camOk` に分けてある**ので、
版を上げても消えない。これを一緒に消すと、上げるたびに自動起動が1回分効かなくなる
（2026-09-10 にこれで「Android で自動起動しない」と誤認した）。

### 実機を adb で直接調べる

USB 接続していれば、画面を撮って読むのが一番確実。

```
adb -s <id> exec-out screencap -p > x.png   # 画面を撮る
adb -s <id> shell input tap <x> <y>         # 操作する
```

- **DevTools 経由（`adb forward tcp:9222 ...`）は応答が無く使えなかった**（2026-09-10）
- 🔴 **`am force-stop` は WebAPK のパッケージだけでは足りない。**
  Chrome 本体（`com.android.chrome`）も落とさないと「前の状態が復帰しただけ」になる

## 注意

- **診断バー（状態・解像度・FPS・カメラ）は既定で非表示。** 画面を広く使うため。
  出したいときは「調整」→「診断バー」にチェック。解像度と鏡像表示の設定も調整パネル内にある。
- **ローカルで CSS や JS を変えたら、まずブラウザのキャッシュを疑う。**
  `python -m http.server` は `Cache-Control` を返さないため、古いファイルが効き続ける。
  `await fetch('css/style.css', {cache:'reload'})` してからリロードすること。
- 🔴 **公開側も同じ罠がある。GitHub Pages は `Cache-Control: max-age=600` を返す。**
  Service Worker の取得を条件付きリクエスト（`cache: 'no-cache'`）にして対処済み
  （2026-09-10）。それ以前は **push した更新が実機に最大10分届かなかった**。
  実機で「直したのに変わらない」ときは、まずこれを疑う。
- 🔴 **実機を adb で操作するときは、毎回 `screencap` で画面を確認してからタップする。**
  座標を固定して連続タップすると、画面ロックや別アプリに当たって事故る
  （2026-09-10 に Google レンズを開いて権限ダイアログを出した）。
- 🔴 **iOS の長押しメニュー（callout）は `contextmenu` の `preventDefault` では止まらない。**
  画面の長押しは「補正前と見比べる」操作に使っているが、iOS ではコピー・画像保存の
  メニューが割り込む。`css/style.css` の先頭で `-webkit-touch-callout: none` を掛けて切ってある。
  **撮影結果の画像（`#pv-img`）だけは長押しを残している**（保存の最後の手段として案内しているため）。
- 🔴 **ブラウザのバーを消せるのは「ホーム画面に追加してそこから起動した場合」だけ。**
  URL を直接ブラウザで開いている限り、上下のバーは Web ページ側からは消せない（ブラウザの仕様）。
  実機で全画面を確認するときは、必ずホーム画面のアイコンから起動すること。
- **Service Worker の登録は、このデスクトップアプリ内蔵ブラウザでは検証できない**（2026-09-07 確認）。
  `sw.js` の取得は 200 で成功するのに登録だけが `An unknown error occurred when fetching the script`
  で失敗する。内蔵ブラウザ側の制約なので、**実 Chrome で確認すること**。
- ローカル検証で `127.0.0.1:8000` に SW を登録したら、**終わったら必ず解除する**。
  このポートは voiceapp など他プロジェクトでも使うため、残すと別プロジェクトの表示を乗っ取る。
- **このフォルダは Dropbox 同期下にあり、`.git` も同期される。**
  医院PC と家KS で**同時に git 操作をしないこと**。作業前に Dropbox の同期完了を確認する。
- コミットには GitHub の匿名アドレス（`<id>+ks-kit@users.noreply.github.com`）を使う設定が
  リポジトリローカルに入れてある。公開リポジトリの履歴にメールアドレスを残さないため。**変更しない。**
- リポジトリは **public**。無料プランの GitHub Pages は公開リポジトリからしか配信できないため。
  写真が端末外に出ない構造なので、個人情報のリスクはない。
- `*.bak` は `.gitignore` で除外済み。**`_bak/` にまとめてある。**

## ファイル構成

```
index.html          画面
css/style.css       スタイル
js/app.js           カメラ制御・補正パラメータ・撮影・保存
js/renderer.js      WebGL2 の描画パイプライン（4パス構成）
js/shaders.js       補正シェーダ本体（GLSL）
manifest.json       PWA の定義（全画面表示・アイコン・起動URL）
sw.js               Service Worker（オフライン起動・インストール要件）
icons/              ホーム画面用アイコン（192/512/maskable/apple-touch）
test-shader.html    シェーダ検証ページ
face-test.html      顔検出の検証ページ（本体に触らず単独で試せる）
js/face.js          顔検出とマスク生成
js/face-test.js     顔検出の検証用
仕様書.md            設計の経緯・実測値・ハマった点の記録
README.md           公開用の説明
_bak/               編集前のバックアップ（.gitignore 済み・公開されない）
```

`.bak` は直下に散らかさず `_bak/` にまとめてある（2026-09-08 に整理）。

### 補正の仕組み（要点）

描画は 7 パス。

1. 映像 → origFBO（鏡像と上下の向きをここで確定）
2. バイラテラルフィルタ・横
3. バイラテラルフィルタ・縦
4. 肌だけを取り出して長辺 128 へ（色にマスクを掛け、マスクを alpha に入れる）
5. ガウス・横
6. ガウス・縦 → 「周囲の肌の平均色」ができる
7. 合成（元画像とぼかしを混ぜ、平均より暗い所を持ち上げ、トーンを整える）

**ぼかしだけを低い解像度で計算している**のが要点。ぼかしは低い周波数の成分しか持たないので
縮小しても見た目が変わらない一方、計算量は面積に比例するため、ここを半分にすると負荷が 1/4 になる。
合成は出力解像度のまま行うので、目や髪のディテールは落ちない。

さらに**プレビューと撮影で解像度を分けている**。プレビューは長辺 1920 まで落とし、
シャッターを切った瞬間だけカメラのフル解像度（4K）に切り替えて処理し直す。
これをやる前は 4K で 30fps しか出なかった。

### 実機なしでシェーダを検証する

`test-shader.html` をブラウザで開くだけでよい。合成したテスト画像に補正をかけた結果が並ぶ。

- 左上の赤・右下の青の位置が変わっていなければ、**座標系の反転は起きていない**
- 中央の暗い横線がぼけずに残っていれば、**輪郭を保つ処理が効いている**
- 「肌の判定」で肌色の面だけが白ければ、**マスクが正しい**

座標系の反転はこの構成で最もバグりやすい箇所なので、シェーダを触ったら必ずここで確認する。

**髭・くま・くすみは「周りの肌より、その場所だけ暗い」という共通の性質で扱っている。**
バイラテラルでは低い周波数の暗さは消せないので、パス 4〜6 で作った「周囲の肌の平均色」より
暗い分を持ち上げる。詳細と、唇を守る仕組みは `仕様書.md` の「補正の見直し B」にある。

**肌マスクの値や、補正・色味の効き具合を数値で確かめたいときは、
色パッチを並べた canvas を `Renderer` に流して `readPixels` で読み戻す。**
実機もカメラも要らない。手順は `仕様書.md` の「補正の見直し A」にある。
`skinMask` は純粋な色の関数なので、同じ式を Python に写せば、
実装前にしきい値の候補を比較できる。

この方法で確かめられるのは**「どこに効くか」まで**。
**「どれくらい効かせるべきか」は実機の顔でしか決まらない。**
髭・くまの既定値は、合成パッチで詰めた 0.40 が実機では 0.10 だった（4倍のずれ）。
